# Modulo Projetos - Implementation Plan

> **For agentic workers:** implementar task por task, com commits pequenos. Como ha tabela nova, RLS, storage e a segunda excecao de visibilidade do cliente no app, este plano deve passar por revisao previa do Claude Code em modo somente leitura antes da implementacao.

**Commit-base:** `61145e7`

**Responsavel pela implementacao futura:** Codex.

**Revisor previo:** Claude Code, somente leitura.

**Spec de referencia:** `docs/superpowers/specs/2026-07-18-projetos-design.md`

**Goal:** Criar o modulo Projetos, um repositorio de documentos da obra (projeto executivo,
memorial, administrativo) com versionamento simples — nova revisao vira automaticamente a
atual, historico preservado — visivel em modo leitura para o cliente e gerenciavel por
admin/equipe com o modulo `projetos`.

**Architecture:** O modulo nasce com duas tabelas (`projetos_documentos`,
`projetos_revisoes`), um enum de categoria, RLS por obra e permissao por modulo `projetos`,
mais um bucket de Storage privado so-PDF (`projetos`) com policy de leitura estendida ao
papel `cliente` — diferente da maioria dos buckets do app, que hoje excluem cliente. A UI usa
uma pagina de lista + uma pagina de detalhe em React, rotas protegidas seguindo o padrao dos
demais modulos, com a rota liberada tambem para `cliente` (leitura).

**Tech Stack:** Supabase/Postgres + RLS + Storage; React 19 + TypeScript + Vite 6; CSS
modules existentes.

## Global Constraints

- Documento pertence obrigatoriamente a uma obra; sem vinculo com unidade/etapa/servico.
- Todo documento nasce com pelo menos uma revisao — nao existe documento vazio.
- So PDF e aceito no upload (`application/pdf`).
- Codigo de revisao (`revisao`) e texto livre digitado pelo usuario — nao gerado automaticamente.
- Nova revisao vira `atual = true` automaticamente; a revisao anterior do mesmo documento vira
  `atual = false` na mesma transacao.
- Cliente enxerga a lista e baixa qualquer revisao (atual ou historica) — leitura apenas, sem
  botoes de escrita.
- Escrita (criar documento, subir revisao, editar, inativar) restrita a admin e equipe com o
  modulo `projetos`.
- Isolamento por obra via `pode_acessar_obra(obra_id)` com policy `AS RESTRICTIVE`, criada
  na MESMA migracao inicial — nunca como retrofit posterior (ver achado critico da revisao
  previa de Tarefas, `docs/superpowers/plans/2026-07-18-tarefas.md`).
- Bucket de Storage precisa ser adicionado a policy RESTRICTIVE existente
  `isolamento_obra_storage` (`supabase/migrations/20260717_isolamento_usuario_obra.sql`) —
  sem isso, os arquivos do bucket `projetos` ficam SEM isolamento entre obras, porque essa
  policy so aplica a restricao aos buckets explicitamente listados nela.
- Soft delete segue a regra do projeto: SELECT precisa permitir ver linha inativa para quem
  pode editar `projetos`, evitando bloqueio de `ativo = false`.
- Toda resposta de erro do Supabase deve ser tratada na tela.
- Sem `window.confirm`; usar dialogo existente do app (`useConfirmDialog`).
- Enum novo em `modulo_app` precisa ser aplicado em transacao separada do resto da migracao
  (Postgres nao permite usar um valor de enum recem-criado na mesma transacao — ver nota no
  Step 8 do Task 1, aprendida na aplicacao de Tarefas em 18/07/2026).
- Funcoes de trigger `SECURITY DEFINER` precisam de `REVOKE ALL ... FROM PUBLIC, anon,
  authenticated` para nao ficarem expostas como RPC publico (ver achado da auditoria de
  17/07/2026 e do hardening de Tarefas em 18/07/2026).

## Arquivos previstos

- Criar: `supabase/migrations/20260718_projetos.sql`
- Modificar: `src/lib/supabase.ts`
- Criar: `src/pages/Projetos.tsx`
- Criar: `src/pages/Projetos.module.css`
- Modificar: `src/App.tsx`
- Modificar: `src/components/Layout.tsx`
- Modificar: `src/pages/Usuarios.tsx`
- Modificar: documentacao final do modulo, apos implementacao e aceite tecnico

---

## Task 1: Banco, enum, tabelas, RLS, storage e permissao do modulo

**Files:**

- Create: `supabase/migrations/20260718_projetos.sql`
- Modify: `src/lib/supabase.ts`

**Interfaces:**

- Produces: enum `categoria_documento_projeto`
- Produces: tabela `projetos_documentos`
- Produces: tabela `projetos_revisoes`
- Produces: modulo `projetos` em `modulo_app`
- Produces: helper `pode_editar_projetos()`
- Produces: bucket de Storage `projetos`

- [ ] **Step 1: Adicionar `projetos` ao enum `modulo_app`**

```sql
ALTER TYPE modulo_app ADD VALUE IF NOT EXISTS 'projetos';
```

- [ ] **Step 2: Criar enum de categoria**

```sql
CREATE TYPE categoria_documento_projeto AS ENUM (
  'projeto_executivo',
  'memorial',
  'administrativo'
);
```

- [ ] **Step 3: Criar tabela `projetos_documentos`**

```sql
CREATE TABLE projetos_documentos (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  obra_id     UUID NOT NULL REFERENCES obras(id) ON DELETE CASCADE,
  titulo      TEXT NOT NULL,
  categoria   categoria_documento_projeto NOT NULL,
  descricao   TEXT,
  ativo       BOOLEAN NOT NULL DEFAULT true,
  criado_em   TIMESTAMPTZ NOT NULL DEFAULT now(),
  criado_por  UUID NOT NULL DEFAULT auth.uid() REFERENCES perfis_usuario(id),
  CONSTRAINT projetos_documentos_titulo_not_blank CHECK (btrim(titulo) <> '')
);

CREATE INDEX idx_projetos_documentos_obra ON projetos_documentos(obra_id) WHERE ativo;
CREATE INDEX idx_projetos_documentos_categoria ON projetos_documentos(obra_id, categoria) WHERE ativo;
```

- [ ] **Step 4: Criar tabela `projetos_revisoes`**

```sql
CREATE TABLE projetos_revisoes (
  id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  documento_id  UUID NOT NULL REFERENCES projetos_documentos(id) ON DELETE CASCADE,
  revisao       TEXT NOT NULL,
  path          TEXT NOT NULL,
  observacao    TEXT,
  atual         BOOLEAN NOT NULL DEFAULT true,
  criado_em     TIMESTAMPTZ NOT NULL DEFAULT now(),
  criado_por    UUID NOT NULL DEFAULT auth.uid() REFERENCES perfis_usuario(id),
  CONSTRAINT projetos_revisoes_revisao_not_blank CHECK (btrim(revisao) <> '')
);

CREATE INDEX idx_projetos_revisoes_documento ON projetos_revisoes(documento_id, criado_em DESC);
CREATE UNIQUE INDEX idx_projetos_revisoes_unica_atual ON projetos_revisoes(documento_id) WHERE atual;
```

O indice unico `idx_projetos_revisoes_unica_atual` garante no banco que nunca existam duas
revisoes `atual = true` do mesmo documento ao mesmo tempo, mesmo que o trigger do Step 5 tenha
algum bug — mesmo padrao de defesa em profundidade usado em
`idx_medicoes_itens_unico_por_medicao` (`supabase/migrations/20260713_fase7_medicoes_travas.sql`).

- [ ] **Step 5: Trigger para marcar a nova revisao como atual**

```sql
CREATE OR REPLACE FUNCTION marcar_revisao_atual()
RETURNS TRIGGER
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
BEGIN
  UPDATE projetos_revisoes
  SET atual = false
  WHERE documento_id = NEW.documento_id
    AND id <> NEW.id
    AND atual = true;
  RETURN NEW;
END;
$$;

CREATE TRIGGER trg_marcar_revisao_atual
  AFTER INSERT ON projetos_revisoes
  FOR EACH ROW EXECUTE FUNCTION marcar_revisao_atual();
```

Este trigger roda `AFTER INSERT`, entao o `INSERT` da nova revisao ja aconteceu com
`atual = true` (valor padrao da coluna); o trigger so precisa desmarcar as outras. Isso evita
que o cliente precise fazer duas chamadas separadas (inserir a revisao E desmarcar a antiga).

- [ ] **Step 6: Habilitar RLS e criar `pode_editar_projetos()`**

```sql
ALTER TABLE projetos_documentos ENABLE ROW LEVEL SECURITY;
ALTER TABLE projetos_revisoes ENABLE ROW LEVEL SECURITY;

CREATE OR REPLACE FUNCTION pode_editar_projetos()
RETURNS boolean
LANGUAGE sql STABLE
SET search_path = public
AS $$
  SELECT meu_papel() = 'admin'
    OR (meu_papel() = 'equipe' AND 'projetos' = ANY(meus_modulos()))
$$;
```

- [ ] **Step 7: Policies de `projetos_documentos` e `projetos_revisoes`**

```sql
-- Isolamento por obra desde o dia 1 (nao como retrofit).
CREATE POLICY isolamento_obra ON projetos_documentos AS RESTRICTIVE FOR ALL TO authenticated
  USING (pode_acessar_obra(obra_id))
  WITH CHECK (pode_acessar_obra(obra_id));

CREATE POLICY isolamento_obra ON projetos_revisoes AS RESTRICTIVE FOR ALL TO authenticated
  USING (EXISTS (SELECT 1 FROM projetos_documentos d WHERE d.id = documento_id AND pode_acessar_obra(d.obra_id)))
  WITH CHECK (EXISTS (SELECT 1 FROM projetos_documentos d WHERE d.id = documento_id AND pode_acessar_obra(d.obra_id)));

-- Leitura: todos os papeis, inclusive cliente (segunda excecao do app, apos Definicoes de Projeto).
-- Escrita: admin, ou equipe com o modulo 'projetos'.
CREATE POLICY projetos_documentos_select ON projetos_documentos FOR SELECT TO authenticated
  USING (ativo = true OR pode_editar_projetos());
CREATE POLICY projetos_documentos_insert ON projetos_documentos FOR INSERT TO authenticated
  WITH CHECK (pode_editar_projetos());
CREATE POLICY projetos_documentos_update ON projetos_documentos FOR UPDATE TO authenticated
  USING (pode_editar_projetos())
  WITH CHECK (pode_editar_projetos());

CREATE POLICY projetos_revisoes_select ON projetos_revisoes FOR SELECT TO authenticated
  USING (EXISTS (
    SELECT 1 FROM projetos_documentos d
    WHERE d.id = documento_id AND (d.ativo = true OR pode_editar_projetos())
  ));
CREATE POLICY projetos_revisoes_insert ON projetos_revisoes FOR INSERT TO authenticated
  WITH CHECK (pode_editar_projetos() AND EXISTS (
    SELECT 1 FROM projetos_documentos d WHERE d.id = documento_id AND d.ativo = true
  ));
```

Nao ha policy de UPDATE/DELETE em `projetos_revisoes`: revisao e append-only no MVP (corrigir
= subir nova revisao), igual ao padrao ja usado em `tarefas_comentarios`.

Note que a policy de SELECT de `projetos_documentos` NAO restringe por papel (nem exclui
`cliente`) — isso e intencional, e a forma como o cliente ve o modulo em modo leitura, igual
ao padrao ja usado em `definicoes_projeto`.

- [ ] **Step 8: Criar o bucket de Storage e suas policies**

```sql
INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
VALUES ('projetos', 'projetos', false, 20971520, ARRAY['application/pdf'])
ON CONFLICT (id) DO NOTHING;

-- Leitura: todos os papeis, inclusive cliente. Escrita: admin/equipe com o modulo.
CREATE POLICY projetos_storage_select ON storage.objects FOR SELECT TO authenticated
  USING (bucket_id = 'projetos');
CREATE POLICY projetos_storage_insert ON storage.objects FOR INSERT TO authenticated
  WITH CHECK (bucket_id = 'projetos' AND pode_editar_projetos());
```

**Passo critico — nao pular:** a policy RESTRICTIVE `isolamento_obra_storage`, criada em
`supabase/migrations/20260717_isolamento_usuario_obra.sql`, so aplica isolamento entre obras
aos buckets explicitamente listados nela (`rdo`, `fvs`, `pendencias`, `cotacoes-nf`). Um
bucket novo que NAO for adicionado a essa lista fica **sem isolamento entre obras** — qualquer
usuario autenticado, de qualquer obra, conseguiria ler/escrever arquivos de qualquer obra
nesse bucket, apesar da policy `projetos_storage_select`/`projetos_storage_insert` acima (essas
so checam papel, nunca obra). E preciso recriar essa policy adicionando `projetos`:

```sql
DROP POLICY isolamento_obra_storage ON storage.objects;

CREATE POLICY isolamento_obra_storage ON storage.objects AS RESTRICTIVE FOR ALL TO authenticated
USING (
  bucket_id NOT IN ('rdo','fvs','pendencias','cotacoes-nf','projetos')
  OR CASE
    WHEN bucket_id IN ('rdo','fvs','pendencias','projetos') THEN
      split_part(name,'/',1) ~* '^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$'
      AND pode_acessar_obra(split_part(name,'/',1)::UUID)
    WHEN bucket_id = 'cotacoes-nf' THEN
      split_part(name,'/',1) ~* '^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$'
      AND EXISTS (
        SELECT 1 FROM pedidos_compra p
        WHERE p.id=split_part(name,'/',1)::UUID AND pode_acessar_obra(p.obra_id)
      )
    ELSE false
  END
)
WITH CHECK (
  bucket_id NOT IN ('rdo','fvs','pendencias','cotacoes-nf','projetos')
  OR CASE
    WHEN bucket_id IN ('rdo','fvs','pendencias','projetos') THEN
      split_part(name,'/',1) ~* '^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$'
      AND pode_acessar_obra(split_part(name,'/',1)::UUID)
    WHEN bucket_id = 'cotacoes-nf' THEN
      split_part(name,'/',1) ~* '^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$'
      AND EXISTS (
        SELECT 1 FROM pedidos_compra p
        WHERE p.id=split_part(name,'/',1)::UUID AND pode_acessar_obra(p.obra_id)
      )
    ELSE false
  END
);
```

Isso exige que todo `path` gravado em `projetos_revisoes.path` comece com `obra_id/` (UUID)
como primeira pasta — mesmo padrao usado em RDO/FVS/Pendencias (ex.:
`{obra_id}/{documento_id}/{revisao}.pdf`).

- [ ] **Step 9: Hardening da funcao SECURITY DEFINER**

```sql
REVOKE ALL ON FUNCTION marcar_revisao_atual() FROM PUBLIC, anon, authenticated;
```

Isso impede que a funcao de trigger seja chamada diretamente via `/rest/v1/rpc/marcar_revisao_atual`
por um usuario anonimo ou autenticado; o disparo automatico do trigger continua funcionando
normalmente (o REVOKE nao afeta isso — ver `20260717_isolamento_usuario_obra.sql` e o
hardening aplicado em Tarefas em 18/07/2026 como referencia).

- [ ] **Step 10: Tipos TypeScript**

Adicionar em `src/lib/supabase.ts`:

```typescript
export type CategoriaDocumentoProjeto = 'projeto_executivo' | 'memorial' | 'administrativo'

export interface ProjetoDocumento {
  id: string
  obra_id: string
  titulo: string
  categoria: CategoriaDocumentoProjeto
  descricao: string | null
  ativo: boolean
  criado_em: string
  criado_por: string
}

export interface ProjetoRevisao {
  id: string
  documento_id: string
  revisao: string
  path: string
  observacao: string | null
  atual: boolean
  criado_em: string
  criado_por: string
}
```

Tambem adicionar `'projetos'` na union type `ModuloApp` existente (mesmo arquivo, mesmo
padrao usado para `'tarefas'` em 18/07/2026).

- [ ] **Step 11: Build**

```bash
npm run build
```

- [ ] **Step 12: Commit**

```bash
git add supabase/migrations/20260718_projetos.sql src/lib/supabase.ts
git commit -m "feat: estrutura dados, RLS e storage de projetos"
```

**Nota de aplicacao (nao e um step de codigo, e um lembrete para quem for aplicar em
producao):** o `ALTER TYPE modulo_app ADD VALUE 'projetos'` do Step 1 precisa ser aplicado em
uma transacao separada do restante da migracao — o Postgres nao permite usar um valor de enum
recem-criado na mesma transacao em que ele foi adicionado (erro `55P04: unsafe use of new
value`). Isso ja aconteceu na aplicacao de `definicoes_projeto` e de `tarefas`; aplicar o Step
1 sozinho primeiro, depois o restante do arquivo.

---

## Task 2: Pagina Projetos - lista, filtros e novo documento

**Files:**

- Create: `src/pages/Projetos.tsx`
- Create: `src/pages/Projetos.module.css`

**Interfaces:**

- Consumes: `ProjetoDocumento`, `ProjetoRevisao`, `CategoriaDocumentoProjeto`.
- Consumes: `useObra`, `useAuth`, `supabase`.

- [ ] **Step 1: Criar a pagina base**

Criar pagina com:

- carregamento por `obraAtiva.id`, filtrando `projetos_documentos` com `ativo = true`;
- lista ordenada por `titulo`;
- estados de carregando, vazio, erro e filtro sem resultado;
- bloqueio de acesso: se `perfil.papel === 'cliente'`, a pagina so mostra leitura (sem botao
  "Novo documento" nem acoes de escrita); se `perfil.papel !== 'cliente'` e o usuario nao tem
  o modulo `projetos` nem e admin, mostrar mensagem "Voce nao tem permissao para acessar
  Projetos" (mesmo padrao usado em `Tarefas.tsx`).

- [ ] **Step 2: Implementar filtros**

Filtros:

- categoria (`projeto_executivo` | `memorial` | `administrativo` | todas);
- busca por titulo.

- [ ] **Step 3: Para cada documento, buscar a revisao atual**

Ao carregar a lista de documentos, buscar em paralelo a revisao com `atual = true` de cada
documento (`supabase.from('projetos_revisoes').select('*').in('documento_id', ids).eq('atual', true)`),
para exibir o codigo da revisao atual e a data no card da lista.

- [ ] **Step 4: Formulario de novo documento**

Campos (visivel so para quem `podeEditar`):

- titulo obrigatorio;
- categoria obrigatoria (select com as 3 opcoes);
- descricao opcional;
- arquivo PDF obrigatorio (input `type="file"` com `accept="application/pdf"`);
- codigo da primeira revisao obrigatorio (texto livre, ex.: "R00").

Validar no frontend antes de enviar:

- titulo preenchido;
- categoria selecionada;
- arquivo selecionado e do tipo `application/pdf` (checar `file.type === 'application/pdf'`,
  com mensagem clara se nao for);
- codigo de revisao preenchido;
- obra ativa existente.

- [ ] **Step 5: Criar documento + primeira revisao**

Fluxo ao salvar:

1. `INSERT` em `projetos_documentos` (retornando o `id` criado);
2. `upload` do arquivo no bucket `projetos`, path `${obraAtiva.id}/${documentoId}/${Date.now()}-${revisao}.pdf`;
3. `INSERT` em `projetos_revisoes` com o `path` retornado do upload.

Se o passo 2 ou 3 falhar depois do passo 1 ja ter criado o documento, exibir erro claro e
deixar o documento (sem revisao) visivel na lista — o usuario pode entrar no detalhe e subir a
revisao de novo (Task 3 cobre "nova revisao" para documento existente, reaproveitavel aqui). Nao
tentar reverter o `INSERT` do documento no frontend.

- [ ] **Step 6: Build**

```bash
npm run build
```

- [ ] **Step 7: Commit**

```bash
git add src/pages/Projetos.tsx src/pages/Projetos.module.css
git commit -m "feat: cria lista e cadastro de documentos de projetos"
```

---

## Task 3: Detalhe do documento - historico de revisoes, editar e inativar

**Files:**

- Modify: `src/pages/Projetos.tsx`
- Modify: `src/pages/Projetos.module.css`

**Interfaces:**

- Consumes: tabelas `projetos_documentos` e `projetos_revisoes`.

- [ ] **Step 1: Detalhe do documento**

Ao selecionar um documento na lista, mostrar:

- titulo, categoria, descricao;
- revisao atual em destaque, com botao "Abrir" (gera signed URL via
  `supabase.storage.from('projetos').createSignedUrl(path, 300)` e abre em nova aba — bucket
  e privado, entao precisa de signed URL, nao URL publica);
- historico de revisoes anteriores (`atual = false`), ordenado por `criado_em` decrescente,
  cada uma com seu proprio botao "Abrir" (signed URL).

- [ ] **Step 2: Nova revisao**

Botao "Nova revisao" (oculto para quem nao `podeEditar`): abre formulario com arquivo PDF
obrigatorio, codigo de revisao obrigatorio e observacao opcional. Ao salvar: upload no bucket
seguido de `INSERT` em `projetos_revisoes` (o trigger do Task 1 Step 5 cuida de desmarcar a
revisao anterior como atual).

- [ ] **Step 3: Editar documento**

Botao "Editar" (oculto para quem nao `podeEditar`): permite alterar titulo, categoria e
descricao via `UPDATE` em `projetos_documentos`. Nao permite editar revisoes existentes.

- [ ] **Step 4: Inativar documento**

Botao "Inativar" (oculto para quem nao `podeEditar`), com confirmacao via `useConfirmDialog`
(sem `window.confirm`) explicando que o documento e todas as revisoes ficam preservados no
banco e no storage, so deixam de aparecer na lista. `UPDATE projetos_documentos SET ativo =
false WHERE id = ...`.

- [ ] **Step 5: Regras de permissao na UI**

- cliente nunca ve botoes de escrita (Novo documento, Nova revisao, Editar, Inativar);
- equipe sem o modulo `projetos` nao acessa a pagina (bloqueada no Task 2 Step 1);
- as regras de UI sao conveniencia; a trava real fica na RLS.

- [ ] **Step 6: Build**

```bash
npm run build
```

- [ ] **Step 7: Commit**

```bash
git add src/pages/Projetos.tsx src/pages/Projetos.module.css
git commit -m "feat: adiciona detalhe, nova revisao e inativacao de documentos"
```

---

## Task 4: Rotas, menu e permissao em Usuarios

**Files:**

- Modify: `src/App.tsx`
- Modify: `src/components/Layout.tsx`
- Modify: `src/pages/Usuarios.tsx`

**Interfaces:**

- Consumes: tabela `projetos_documentos`.
- Produces: rota `/projetos`.

- [ ] **Step 1: Rota**

Adicionar rota protegida `/projetos` em `src/App.tsx`, seguindo o padrao dos demais modulos
(`const Projetos = lazy(() => import('./pages/Projetos'))`, `<Route path="projetos"
element={<Projetos />} />`). A rota fica acessivel para os tres papeis (a pagina em si decide
o que renderizar conforme o papel, igual a `Tarefas.tsx` decide para cliente hoje — aqui o
cliente ve conteudo real em vez da mensagem de "uso interno").

- [ ] **Step 2: Menu**

Adicionar **Projetos** no menu lateral (`src/components/Layout.tsx`), com visibilidade:

- admin: sempre;
- equipe com modulo `projetos`: sim;
- cliente: sempre (leitura), sem depender de modulo marcado — mesmo padrao ja usado para
  **Definições de Projeto** no mesmo arquivo.

- [ ] **Step 3: Permissao em Usuarios**

Adicionar `projetos: 'Projetos'` em `MODULOS_LABELS` (`src/pages/Usuarios.tsx`), mesmo padrao
usado para `tarefas` em 18/07/2026, para que o admin possa marcar o modulo `projetos` para um
usuario `equipe`.

- [ ] **Step 4: Build**

```bash
npm run build
```

- [ ] **Step 5: Commit**

```bash
git add src/App.tsx src/components/Layout.tsx src/pages/Usuarios.tsx
git commit -m "feat: integra projetos ao menu e as rotas"
```

---

## Task 5: Validacao final, deploy e documentacao

**Files:**

- Modify: `docs/fase7_projetos.md` ou criar documento equivalente, se ainda nao existir
- Modify: `AGENTS.md` e `CLAUDE.md`, se o modulo for entregue para teste real

- [ ] **Step 1: Build final**

```bash
npm run build
```

- [ ] **Step 2: Teste manual desktop**

1. Abrir `/projetos` como admin.
2. Criar documento com PDF, categoria e revisao (ex.: "R00").
3. Tentar salvar sem arquivo ou sem titulo e confirmar bloqueio.
4. Tentar subir um arquivo que nao seja PDF e confirmar bloqueio.
5. Abrir o documento, subir uma nova revisao (ex.: "R01") e confirmar que ela vira a atual e a
   R00 aparece no historico como superada, ainda abrivel.
6. Editar titulo/categoria/descricao.
7. Inativar o documento e confirmar que ele some da lista.
8. Filtrar por categoria e buscar por titulo.

- [ ] **Step 3: Teste manual celular**

1. Abrir `/projetos` no PWA/mobile.
2. Criar documento, subir revisao.
3. Abrir detalhe, abrir um PDF (signed URL deve funcionar no navegador mobile).
4. Conferir se filtros e formulario nao quebram layout (aplicar o mesmo cuidado de `min-width:
   0` em grids/flex que corrigiu o overflow de Tarefas em 18/07/2026, se a tela usar grid de
   colunas).

- [ ] **Step 4: Teste de permissao**

1. Admin: cria, edita, sobe revisao, inativa.
2. Equipe com `projetos`: cria, edita, sobe revisao, inativa.
3. Equipe sem `projetos`: nao acessa a pagina (ve mensagem de sem permissao).
4. Cliente: ve a lista, abre/baixa qualquer revisao, NAO ve nenhum botao de escrita, mesmo
   tentando acessar `/projetos` diretamente pela URL.

- [ ] **Step 5: Teste de isolamento entre obras**

1. Com um usuario vinculado a apenas uma obra, confirmar que documentos de outra obra nao
   aparecem na lista nem no Storage (tentar montar a URL do arquivo de outra obra manualmente,
   se possivel, e confirmar que a signed URL falha).

- [ ] **Step 6: Revisar diff final**

```bash
git diff --check
git status --short --branch
```

- [ ] **Step 7: Commit de documentacao, se houver**

```bash
git add docs/fase7_projetos.md AGENTS.md CLAUDE.md
git commit -m "docs: registra modulo projetos"
```

- [ ] **Step 8: Publicar e verificar deploy**

```bash
git push
```

Verificar app publicado na Vercel e confirmar que `/projetos` carrega para os tres papeis.

---

## Prompt para revisao previa do Claude Code

```text
Leia integralmente CLAUDE.md, docs/colaboracao-codex-claude.md,
docs/sequencia-trabalho-codex-claude.md,
docs/superpowers/specs/2026-07-18-projetos-design.md e
docs/superpowers/plans/2026-07-18-projetos.md.

Responsavel pela implementacao futura: Codex.
Sua atuacao nesta etapa e SOMENTE LEITURA: nao altere arquivos, nao aplique migracoes,
nao faca commit, push ou deploy.

Escopo a revisar: modulo Projetos, MVP com repositorio de documentos versionado por obra,
categorias fixas, revisao atual automatica, cliente em modo leitura (segunda excecao do app
a regra geral de "cliente nao ve"), storage privado so-PDF com policy de leitura estendida
ao cliente.

Commit-base: 61145e7.

Revise o plano antes da implementacao verificando:
1. modelo de dados e indice unico de revisao atual;
2. RLS e isolamento por obra em tabelas E no bucket de storage (atencao especial a
   modificacao da policy isolamento_obra_storage existente);
3. permissao admin/equipe/cliente, incluindo a leitura liberada ao cliente;
4. atomicidade da troca de revisao atual (trigger vs. duas escritas separadas do cliente);
5. hardening da funcao SECURITY DEFINER de trigger;
6. soft delete;
7. integracao com Dashboard/menu;
8. riscos de regressao mobile (grids sem min-width:0);
9. criterios de aceite.

Classifique achados como critico, alto, medio ou baixo. Para cada achado, informe
arquivo/trecho do plano ou spec, evidencia, impacto, correcao recomendada e teste de
validacao. Diferencie defeito comprovado de sugestao.
```
