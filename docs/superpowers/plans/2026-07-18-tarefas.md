# Modulo Tarefas - Implementation Plan

> **For agentic workers:** implementar task por task, com commits pequenos. Como ha tabela nova, RLS e regras de permissao, este plano deve passar por revisao previa do Claude Code em modo somente leitura antes da implementacao.

**Commit-base:** `c07769f`

**Responsavel pela implementacao futura:** Codex.

**Revisor previo:** Claude Code, somente leitura.

**Spec de referencia:** `docs/superpowers/specs/2026-07-18-tarefas-design.md`

**Goal:** Criar o modulo Tarefas para registrar providencias operacionais da obra/escritorio, com responsavel, prazo obrigatorio, prioridade, status, comentarios/historico, vinculo opcional com EAP e contador de atrasadas no Dashboard.

**Architecture:** O modulo nasce com duas tabelas (`tarefas`, `tarefas_comentarios`), enums proprios, RLS por obra e permissao por modulo `tarefas`. A UI usa uma pagina dedicada em React, rotas protegidas pelo mesmo padrao dos modulos existentes, e Dashboard recebe um contador simples de tarefas atrasadas e abertas do usuario. Fotos/anexos ficam fora do MVP.

**Tech Stack:** Supabase/Postgres + RLS; React 19 + TypeScript + Vite 6; CSS modules existentes.

## Global Constraints

- Prazo e obrigatorio no formulario e no banco.
- Toda tarefa pertence obrigatoriamente a uma obra.
- Unidade, etapa e servico sao opcionais.
- Equipe so pode concluir tarefa em que esteja como responsavel.
- Admin pode concluir qualquer tarefa.
- Reabrir tarefa concluida/cancelada e exclusivo do admin.
- Cliente nao ve o modulo e nao acessa a rota/tabelas.
- Comentarios sao append-only no MVP.
- Fotos/anexos ficam para depois do MVP.
- Soft delete deve seguir a regra do projeto: SELECT precisa permitir ver linha inativa para quem pode editar, evitando bloqueio de `ativo = false`.
- Toda resposta de erro do Supabase deve ser tratada na tela.
- Sem `window.confirm`; usar dialogo existente do app.

## Arquivos previstos

- Criar: `supabase/migrations/20260718_tarefas.sql`
- Modificar: `src/lib/supabase.ts`
- Criar: `src/pages/Tarefas.tsx`
- Criar: `src/pages/Tarefas.module.css`
- Modificar: `src/App.tsx`
- Modificar: `src/components/Layout.tsx`
- Modificar: `src/pages/Dashboard.tsx`
- Modificar: `src/pages/Dashboard.module.css`, se necessario
- Modificar: documentacao final do modulo, apos implementacao e aceite tecnico

---

## Task 1: Banco, enums, RLS e permissao do modulo

**Files:**

- Create: `supabase/migrations/20260718_tarefas.sql`
- Modify: `src/lib/supabase.ts`

**Interfaces:**

- Produces: enum `status_tarefa`
- Produces: enum `prioridade_tarefa`
- Produces: tabela `tarefas`
- Produces: tabela `tarefas_comentarios`
- Produces: modulo `tarefas` em `modulo_app`, se ainda nao existir
- Produces: helper `pode_editar_tarefas()`

- [ ] **Step 1: Conferir enum `modulo_app` existente**

Verificar se `tarefas` ja existe no enum `modulo_app`. Se nao existir, adicionar via bloco seguro:

```sql
DO $$
BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_enum e
    JOIN pg_type t ON t.oid = e.enumtypid
    WHERE t.typname = 'modulo_app'
      AND e.enumlabel = 'tarefas'
  ) THEN
    ALTER TYPE modulo_app ADD VALUE 'tarefas';
  END IF;
END $$;
```

- [ ] **Step 2: Criar enums**

```sql
CREATE TYPE status_tarefa AS ENUM ('aberta', 'em_andamento', 'concluida', 'cancelada');
CREATE TYPE prioridade_tarefa AS ENUM ('baixa', 'normal', 'alta', 'urgente');
```

Se o projeto ja tiver padrao de `DO $$` para enums idempotentes, seguir o padrao local.

- [ ] **Step 3: Criar tabelas**

Criar `tarefas` com:

- `obra_id NOT NULL`;
- `prazo DATE NOT NULL`;
- campos opcionais `unidade_id`, `etapa_id`, `servico_id`;
- status/prioridade com defaults;
- `responsavel_id`;
- campos de conclusao/cancelamento;
- `ativo`, `criado_por`, `criado_em`, `atualizado_em`.

Criar `tarefas_comentarios` com:

- `tarefa_id`;
- `tipo` (`comentario` ou evento textual controlado pelo app);
- `comentario`;
- `criado_por`, `criado_em`.

- [ ] **Step 4: Constraints de integridade**

Adicionar constraints:

- `titulo` nao vazio;
- `motivo_cancelamento` obrigatorio quando status for `cancelada`;
- `concluida_por/concluida_em` obrigatorios quando status for `concluida`;
- `cancelada_por/cancelada_em` obrigatorios quando status for `cancelada`;
- `unidade_id`, `etapa_id`, `servico_id` precisam pertencer a mesma obra quando preenchidos, se houver helper/padrao local simples para isso.

- [ ] **Step 5: Funcoes e RLS**

Criar `pode_editar_tarefas()` seguindo o padrao de `pode_editar_*` existente:

- `admin` retorna true;
- `equipe` com modulo `tarefas` retorna true;
- cliente retorna false.

RLS:

- SELECT em `tarefas`: usuario autenticado com acesso a obra e (`ativo = true OR pode_editar_tarefas()`), cliente excluido.
- INSERT em `tarefas`: `pode_editar_tarefas()` e obra acessivel.
- UPDATE em `tarefas`: `pode_editar_tarefas()` e obra acessivel, com regras de status:
  - equipe nao conclui tarefa de outro responsavel;
  - reabrir concluida/cancelada so admin;
  - cliente nunca atualiza.
- SELECT em `tarefas_comentarios`: permitido para quem pode ler a tarefa.
- INSERT em `tarefas_comentarios`: permitido para quem pode editar tarefas e ler a tarefa.
- UPDATE/DELETE em comentarios: nao permitir no MVP.

- [ ] **Step 6: Tipos TypeScript**

Adicionar em `src/lib/supabase.ts`:

- `StatusTarefa`;
- `PrioridadeTarefa`;
- `Tarefa`;
- `TarefaComentario`.

- [ ] **Step 7: Build**

```bash
npm run build
```

- [ ] **Step 8: Commit**

```bash
git add supabase/migrations/20260718_tarefas.sql src/lib/supabase.ts
git commit -m "feat: estrutura dados e permissoes de tarefas"
```

---

## Task 2: Pagina Tarefas - lista, filtros e criacao

**Files:**

- Create: `src/pages/Tarefas.tsx`
- Create: `src/pages/Tarefas.module.css`

**Interfaces:**

- Consumes: `Tarefa`, `StatusTarefa`, `PrioridadeTarefa`.
- Consumes: `useObra`.
- Consumes: `supabase`.

- [ ] **Step 1: Criar a pagina base**

Criar pagina com:

- carregamento por `obraAtiva.id`;
- lista ordenada por atrasadas, prazo e criado_em;
- estados de carregando, vazio, erro e filtro sem resultado.

- [ ] **Step 2: Implementar filtros**

Filtros:

- status;
- responsavel;
- prioridade;
- atrasadas;
- minhas tarefas;
- busca por titulo/descricao.

- [ ] **Step 3: Formulario de nova tarefa**

Campos:

- titulo obrigatorio;
- descricao;
- responsavel opcional;
- prazo obrigatorio;
- prioridade;
- unidade/etapa/servico opcionais.

Validar no frontend antes de enviar:

- titulo preenchido;
- prazo preenchido;
- obra ativa existente.

- [ ] **Step 4: Criar tarefa**

Inserir em `tarefas` e, em seguida, criar comentario/evento inicial em `tarefas_comentarios`.

Se alguma etapa falhar, exibir erro. Se a criacao composta precisar ser atomica, avaliar RPC antes da implementacao; isso elevaria o risco e precisa estar claro para Claude Code.

- [ ] **Step 5: Build**

```bash
npm run build
```

- [ ] **Step 6: Commit**

```bash
git add src/pages/Tarefas.tsx src/pages/Tarefas.module.css
git commit -m "feat: cria lista e cadastro de tarefas"
```

---

## Task 3: Detalhe, comentarios e fluxo de status

**Files:**

- Modify: `src/pages/Tarefas.tsx`
- Modify: `src/pages/Tarefas.module.css`

**Interfaces:**

- Consumes: tabelas `tarefas` e `tarefas_comentarios`.

- [ ] **Step 1: Detalhe da tarefa**

Adicionar visualizacao/edicao da tarefa selecionada:

- dados principais;
- vinculos;
- historico/comentarios;
- acoes de status.

- [ ] **Step 2: Comentarios**

Permitir inserir comentario manual.

Nao permitir editar/apagar comentario.

- [ ] **Step 3: Status**

Acoes:

- iniciar: `aberta -> em_andamento`;
- concluir: `aberta/em_andamento -> concluida`;
- cancelar: `aberta/em_andamento -> cancelada`, com motivo obrigatorio;
- reabrir: `concluida/cancelada -> aberta`, apenas admin.

Registrar evento em `tarefas_comentarios` apos mudancas relevantes.

- [ ] **Step 4: Regras de permissao na UI**

- equipe so ve botao de concluir quando for responsavel;
- admin ve concluir em qualquer tarefa;
- reabrir so aparece para admin;
- cliente nao chega na pagina.

As regras de UI sao conveniencia; a trava real fica na RLS.

- [ ] **Step 5: Build**

```bash
npm run build
```

- [ ] **Step 6: Commit**

```bash
git add src/pages/Tarefas.tsx src/pages/Tarefas.module.css
git commit -m "feat: adiciona detalhe e fluxo de tarefas"
```

---

## Task 4: Rotas, menu e Dashboard

**Files:**

- Modify: `src/App.tsx`
- Modify: `src/components/Layout.tsx`
- Modify: `src/pages/Dashboard.tsx`
- Modify: `src/pages/Dashboard.module.css`, se necessario

**Interfaces:**

- Consumes: tabela `tarefas`.
- Produces: rota `/tarefas`.

- [ ] **Step 1: Rota**

Adicionar rota protegida `/tarefas`, seguindo o padrao dos demais modulos.

- [ ] **Step 2: Menu**

Adicionar **Tarefas** no menu lateral e na tela principal, com visibilidade:

- admin;
- equipe com modulo `tarefas`;
- cliente oculto.

- [ ] **Step 3: Dashboard**

Adicionar contador simples:

- tarefas atrasadas da obra;
- tarefas abertas do usuario, se houver como encaixar sem poluir.

Manter visual consistente com os KPI cards existentes.

- [ ] **Step 4: Build**

```bash
npm run build
```

- [ ] **Step 5: Commit**

```bash
git add src/App.tsx src/components/Layout.tsx src/pages/Dashboard.tsx src/pages/Dashboard.module.css
git commit -m "feat: integra tarefas ao menu e dashboard"
```

---

## Task 5: Validacao final, deploy e documentacao

**Files:**

- Modify: `docs/fase7_tarefas.md` ou criar documento equivalente, se ainda nao existir
- Modify: `AGENTS.md` e `CLAUDE.md`, se o modulo for entregue para teste real

- [ ] **Step 1: Build final**

```bash
npm run build
```

- [ ] **Step 2: Teste manual desktop**

1. Abrir `/tarefas`.
2. Criar tarefa com prazo obrigatorio.
3. Tentar salvar sem prazo e confirmar bloqueio.
4. Filtrar por status, responsavel, prioridade e atrasadas.
5. Adicionar comentario.
6. Iniciar, concluir, cancelar e reabrir como admin.
7. Confirmar contador no Dashboard.

- [ ] **Step 3: Teste manual celular**

1. Abrir `/tarefas` no PWA/mobile.
2. Criar tarefa.
3. Abrir detalhe.
4. Comentar.
5. Alterar status.
6. Conferir se filtros e botoes nao quebram layout.

- [ ] **Step 4: Teste de permissao**

1. Admin: cria, edita, conclui qualquer tarefa, cancela e reabre.
2. Equipe com `tarefas`: cria/edita/comenta; conclui apenas tarefa em que e responsavel.
3. Equipe sem `tarefas`: nao ve/acessa o modulo.
4. Cliente: nao ve/acessa o modulo.

- [ ] **Step 5: Revisar diff final**

```bash
git diff --check
git status --short --branch
```

- [ ] **Step 6: Commit de documentacao, se houver**

```bash
git add docs/fase7_tarefas.md AGENTS.md CLAUDE.md
git commit -m "docs: registra modulo tarefas"
```

- [ ] **Step 7: Publicar e verificar deploy**

```bash
git push
```

Verificar app publicado na Vercel e confirmar que `/tarefas` carrega.

---

## Prompt para revisao previa do Claude Code

```text
Leia integralmente CLAUDE.md, docs/colaboracao-codex-claude.md,
docs/sequencia-trabalho-codex-claude.md,
docs/superpowers/specs/2026-07-18-tarefas-design.md e
docs/superpowers/plans/2026-07-18-tarefas.md.

Responsavel pela implementacao futura: Codex.
Sua atuacao nesta etapa e SOMENTE LEITURA: nao altere arquivos, nao aplique migracoes,
nao faca commit, push ou deploy.

Escopo a revisar: modulo Tarefas, MVP com tarefas por obra, prazo obrigatorio,
responsavel opcional, equipe concluindo apenas tarefas em que seja responsavel,
reabertura exclusiva do admin, comentarios append-only, fotos fora do MVP e contador
de atrasadas no Dashboard.

Commit-base: c07769f.

Revise o plano antes da implementacao verificando:
1. modelo de dados;
2. RLS e isolamento por obra;
3. permissao admin/equipe/cliente;
4. regra de conclusao apenas pelo responsavel;
5. reabertura exclusiva do admin;
6. soft delete;
7. necessidade ou nao de RPC transacional para criar tarefa + evento inicial;
8. integracao com Dashboard/menu;
9. riscos de regressao mobile;
10. criterios de aceite.

Classifique achados como critico, alto, medio ou baixo. Para cada achado, informe
arquivo/trecho do plano ou spec, evidencia, impacto, correcao recomendada e teste de
validacao. Diferencie defeito comprovado de sugestao.
```

---

## Revisao previa do Claude Code — 18/07/2026

**Revisor:** Claude Code, modo somente leitura. Nenhum arquivo de codigo/migracao alterado,
nenhum commit criado.

**Commit-base considerado:** `c07769f`. Nada implementado ainda (working tree limpa no
momento da revisao).

**Comparacao feita contra:** padrao real em producao — `supabase/migrations/20260717_isolamento_usuario_obra.sql`,
`20260718_producao_plantas_paredes.sql`, `20260713_fase7_contratos_transicao_status.sql`,
`20260713_fase7_medicoes_travas.sql`, `20260709_fase5_pendencias.sql`,
`20260713_definicoes_projeto.sql`.

### Critico 1 — falta policy RESTRICTIVE de isolamento por obra

- **Modulo/cenario:** Tarefas — equipe vinculado a apenas uma obra.
- **Arquivo/trecho:** Task 1, Step 5 (RLS), linhas ~121-139 deste plano — descreve
  "com acesso a obra" em prosa, sem citar `pode_acessar_obra()` nem uma policy RESTRICTIVE.
- **Evidencia:** desde `20260717_isolamento_usuario_obra.sql`, toda tabela com `obra_id`
  precisa de `CREATE POLICY isolamento_obra ... AS RESTRICTIVE`. O precedente mais recente
  (`20260718_producao_plantas_paredes.sql:80-90`) ja nasce com essa policy na mesma
  migracao que cria a tabela — Tarefas deveria seguir o mesmo padrao desde o dia um.
- **Impacto:** sem essa policy, as policies funcionais do plano so checam
  `pode_editar_tarefas()` (papel/modulo), nao o vinculo `usuarios_obras`. Um `equipe`
  vinculado so a Obra A enxergaria e editaria tarefas da Obra B — quebra o isolamento
  fechado em 17/07/2026.
- **Correcao recomendada:** incluir no Step 5, na mesma migracao:
  ```sql
  CREATE POLICY isolamento_obra ON tarefas AS RESTRICTIVE FOR ALL TO authenticated
    USING (pode_acessar_obra(obra_id)) WITH CHECK (pode_acessar_obra(obra_id));
  CREATE POLICY isolamento_obra ON tarefas_comentarios AS RESTRICTIVE FOR ALL TO authenticated
    USING (EXISTS (SELECT 1 FROM tarefas t WHERE t.id = tarefa_id AND pode_acessar_obra(t.obra_id)))
    WITH CHECK (EXISTS (SELECT 1 FROM tarefas t WHERE t.id = tarefa_id AND pode_acessar_obra(t.obra_id)));
  ```
- **Teste de validacao:** equipe vinculado so a Obra A nao deve ver/criar/editar tarefa
  com `obra_id` da Obra B, nem manipulando a chamada direta ao Supabase.
- **Status:** defeito comprovado de desenho (nao um risco teorico) — o plano nao cobre
  o padrao ja obrigatorio no projeto.

### Critico/Alto 2 — maquina de status descrita em prosa, sem trigger

- **Modulo/cenario:** Tarefas — transicao de status (iniciar/concluir/cancelar/reabrir).
- **Arquivo/trecho:** Task 1 Step 5 e Task 3 Step 3 deste plano.
- **Evidencia:** uma policy UPDATE do Postgres aplica USING a linha antiga e WITH CHECK
  a linha nova separadamente — nao da para comparar `OLD.status -> NEW.status` num unico
  predicado RLS do jeito descrito ("equipe nao conclui tarefa de outro responsavel;
  reabrir so admin"). O mesmo problema ja foi encontrado e corrigido em Contratos:
  `20260713_fase7_contratos_transicao_status.sql` existe porque a versao anterior
  "bloqueava nao-admin, mas nao impedia um admin reverter o status via chamada direta
  a API".
- **Impacto:** sem trigger dedicado, a policy fica frouxa demais (qualquer status->status
  para quem tem `pode_editar_tarefas()`), ou o Codex improvisa logica ad hoc que nao
  cobre todas as transicoes — reproduzindo o bug ja corrigido em Contratos.
- **Correcao recomendada:** `BEFORE UPDATE ON tarefas`, trigger dedicado validando a
  tabela de transicoes (aberta<->em_andamento->concluida/cancelada; concluida/cancelada
  ->aberta somente com `meu_papel()='admin'`), igual ao padrao de
  `restringir_status_contrato()`.
- **Teste de validacao:** via chamada direta (nao so UI), tentar concluida->em_andamento
  sem ser admin, e aberta->concluida pulando em_andamento com responsavel diferente do
  usuario logado; ambos devem falhar mesmo com `pode_editar_tarefas()=true`.
- **Status:** defeito comprovado de desenho.

### Alto 3 — campos de autoria/data de conclusao/cancelamento sao colunas comuns, graveis pelo cliente

- **Modulo/cenario:** Tarefas — conclusao ou cancelamento de tarefa.
- **Arquivo/trecho:** Task 1 Step 3-4 — `concluida_por/concluida_em/cancelada_por/cancelada_em`
  so tem NOT NULL condicional, nao tem valor forcado no servidor.
- **Evidencia:** mesma classe de bug fechada em Medicoes em 13/07
  (`20260713_fase7_medicoes_travas.sql`, item 3 — "valor_bruto/retido/liquido eram
  graveis diretamente").
- **Impacto:** qualquer sessao com `pode_editar_tarefas()` poderia gravar `concluida_por`
  de outra pessoa ou `concluida_em` retroativo, contra CLAUDE.md secao 6 regra 1
  (autoria/data sem excecao).
- **Correcao recomendada:** no mesmo trigger do achado 2, forcar
  `NEW.concluida_por := auth.uid(); NEW.concluida_em := now();` (e analogo para
  cancelamento), sobrescrevendo o que vier do cliente. O mesmo trigger pode inserir a
  linha correspondente em `tarefas_comentarios` (evento automatico), o que tambem resolve
  de graca a atomicidade das duas escritas separadas descritas na Task 2 Step 4 e na
  Task 3 Step 3 (criacao de tarefa + evento inicial; mudanca de status + evento).
- **Teste de validacao:** enviar UPDATE de conclusao com `concluida_por` de outro
  usuario; confirmar que o banco grava o `auth.uid()` real, nao o valor enviado.
- **Status:** defeito comprovado de desenho.

### Medio 4 — reatribuicao de responsavel como escape da regra "so conclui quem e responsavel"

- **Modulo/cenario:** Tarefas — troca de `responsavel_id` antes de concluir.
- **Evidencia:** analogo ao achado ja fechado em Medicoes (reatribuicao de item de
  medicao aprovada). O plano/spec nao definem se `equipe` pode alterar `responsavel_id`
  livremente.
- **Impacto:** se puder, um usuario equipe pode se autoatribuir e concluir em seguida,
  driblando a intencao da regra "so conclui quem e responsavel".
- **Correcao recomendada:** decisao explicita do Rodrigo — aceitar o risco (documentado)
  ou restringir troca de responsavel a admin/ao responsavel atual.
- **Status:** lacuna de decisao de negocio, nao defeito de codigo.

### Medio 5 — vinculo unidade/etapa/servico sem checagem de mesma obra e uma brecha de FK, nao so de UX

- **Modulo/cenario:** Tarefas — vinculo opcional com `etapa_id`/`servico_id`.
- **Evidencia:** checagem de FK no Postgres roda com privilegio do dono da tabela e nao
  passa pelas policies RESTRICTIVE de isolamento — da pra vincular `etapa_id`/`servico_id`
  de outra obra sem erro, mesmo sem acesso de leitura aquela obra.
- **Impacto:** vinculo cruzado entre obras, inconsistente com o isolamento do resto do
  app.
- **Correcao recomendada:** o plano ja marca isso como aberto ("se houver helper/padrao
  simples") — recomendo fechar com trigger antes de liberar o campo na UI, nao deixar
  como TODO.
- **Status:** defeito comprovado (a FK realmente nao e filtrada por RLS), correcao ainda
  em aberto no proprio plano.

### Baixo 6 e 7 — sugestoes, nao bloqueantes

- `tarefas_comentarios.tipo` e TEXT livre sem CHECK; o padrao do projeto para colunas de
  estado usa ENUM (ex.: `status_pendencia`). Sugestao: CHECK com a lista de eventos ja
  listada na spec.
- Step 1 usa bloco `DO $$ ... pg_enum ...` para checar `modulo_app`; o padrao ja em uso
  (`definicoes_projeto`) e `ALTER TYPE modulo_app ADD VALUE IF NOT EXISTS 'tarefas';` —
  mais simples, mesmo efeito.

### Verificado sem achados

Modelo de dados basico (tabelas, tipos, FKs), escopo do MVP, fluxo de telas, integracao
com Dashboard/menu e criterios de aceite.

### Recomendacao

Tratar os achados 1-3 (defeitos comprovados de desenho, nao apenas sugestao) antes da
implementacao pelo Codex. O achado 4 depende de decisao do Rodrigo. O achado 5 ja esta
sinalizado no plano, so precisa deixar de ser um "se houver" e virar item fechado.
